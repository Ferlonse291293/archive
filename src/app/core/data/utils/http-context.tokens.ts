import {HttpContextToken} from '@angular/common/module.d-CnjH8Dlt';

export const NO_CACHE = new HttpContextToken<boolean>(() => false);
export const NO_AUTH = new HttpContextToken<boolean>(() => false);
